const slidingWindow = (arr) => {
  let left = 0,
    ans = 0,
    curr = 0;

  for (let right = 0; right < arr.length; right++) {
    //logic to add right index to curr

    while (WINDOW_CONDITION_BROKEN) {
      left++;
    }

    //update ans
  }

  return ans;
};
